import 'reflect-metadata';
import { ResultType } from '../types';
export declare function Cacheable(timeout?: number, resultType?: ResultType): (target: any, key: string | symbol, descriptor: PropertyDescriptor) => PropertyDescriptor;
